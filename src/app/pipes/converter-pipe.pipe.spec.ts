import { ConverterPipePipe } from './converter-pipe.pipe';

describe('ConverterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConverterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
